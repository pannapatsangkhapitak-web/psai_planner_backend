[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "planner-v2"
version = "0.1.0"
description = "ARAS Connect Planner core domain"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["planner_v2*"]
